# set the values and copy the file into instance folder
DEBUG = False  # Turns on debugging features in Flask
# set a 'SECRET_KEY' to enable the Flask session cookies
SECRET_KEY = '<set the secret key>'
# SQLAlchemy URI
SQLALCHEMY_DATABASE_URI = '<set the DB name>'
SQLALCHEMY_TRACK_MODIFICATIONS = False
# Legacy DB definition. Should be replaced by SQLAlchemy
DATABASE_NAME = '<set the DB name>'
